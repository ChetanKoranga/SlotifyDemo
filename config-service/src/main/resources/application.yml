#management:
#  endpoints:
#    web:
#      exposure:
#        include: "*"
        spring:
          cloud:
            config:
              server:
                git:
                  uri: https://gitlab-gl.stackroute.in/vinayak.horambali/config-service.git